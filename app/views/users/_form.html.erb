<%= form_for(@user) do |f| %>
    <% if @user.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

          <ul>
            <% @user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <div class="field">
      <%= f.label :用户名 %><br>
      <%= f.text_field :loginname %>
    </div>
    <div class="field">
      <%= f.label :密码 %><br>
      <%= f.password_field :password %>
    </div>
    <div class="field">
      <%= f.label :确认密码 %><br>
      <%= f.password_field :password_confirmation %>
    </div>
    <div class="field">
      <%= f.label :联系方式 %><br>
      <%= f.text_field :phone %>
    </div>
    <div class="actions">
      <%= f.submit :value=>"确认",:class=>"btn btn-primary"%>
    </div>
    <% end %>