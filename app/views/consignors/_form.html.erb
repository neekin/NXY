<%= form_for(@consignor) do |f| %>
  <% if @consignor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consignor.errors.count, "error") %> prohibited this consignor from being saved:</h2>

      <ul>
      <% @consignor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :phone %><br>
    <%= f.text_field :phone %>
  </div>
    <div class="field">
      <%= f.label :目的地 %><br>
      <%= f.select  :line_id ,@lines.collect{
                                     |l| [l.destination,l.id]
                             } %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
